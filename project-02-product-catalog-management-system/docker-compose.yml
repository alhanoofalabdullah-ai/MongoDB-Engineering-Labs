version: "3.9"

services:

  mongodb:
    image: mongo:7

    container_name: enterprise-product-catalog-db

    restart: always

    ports:
      - "27017:27017"

    environment:
      MONGO_INITDB_DATABASE: enterprise_product_catalog_db
