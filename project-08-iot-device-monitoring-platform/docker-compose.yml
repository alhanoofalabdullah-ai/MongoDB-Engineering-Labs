version: "3.9"

services:

  mongodb:
    image: mongo:7

    container_name: enterprise-iot-db

    restart: always

    ports:
      - "27017:27017"

    environment:
      MONGO_INITDB_DATABASE: enterprise_iot_db
