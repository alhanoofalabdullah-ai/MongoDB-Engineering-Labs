use enterprise_governance_db;

db.createCollection("metadata_repository");

db.createCollection("data_assets");

db.createCollection("data_quality_rules");

db.createCollection("data_lineage");

db.createCollection("compliance_records");

db.createCollection("master_data");

db.createCollection("data_stewards");
