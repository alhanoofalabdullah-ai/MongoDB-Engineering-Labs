use enterprise_mdm_db;

db.createCollection("customer_master");

db.createCollection("supplier_master");

db.createCollection("product_master");

db.createCollection("golden_records");

db.createCollection("reference_data");

db.createCollection("matching_rules");

db.createCollection("data_quality_scores");
